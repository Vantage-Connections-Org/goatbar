# winget manifest (draft, not submitted)

Submitting to winget means opening a pull request on the public
[microsoft/winget-pkgs](https://github.com/microsoft/winget-pkgs) repository, so it is left for the
maintainer. The manifests below are ready for `winget validate` and `wingetcreate submit`.

Easiest route: `wingetcreate new https://github.com/Vantage-Connections-Org/goatbar/releases/download/v0.1.0/GoatBar-windows-x64.zip`
and compare with these files.
