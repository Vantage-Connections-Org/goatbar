// Plain-text product summary shared by /llms.txt and /llms-full.txt.
export const LLMS_INTRO = "# GoatBar\n\n> GoatBar is a free, open-source (MIT) Windows 10/11 app that shows every running Claude Code and OpenAI Codex CLI chat as a small status square in the Windows taskbar, next to the system tray.\n\n- Status is shown by each square's background: working (dull orange), done and waiting on you (green), needs an answer (blue), idle (collapses to a thin bar).\n- Each square's border is the chat's own colour (Claude Code's /color), so chats are easy to tell apart.\n- Clicking a square brings that chat's window (Zed, Windows Terminal, VS Code, Cursor, and others) to the front.\n- It reads only local files the tools already keep (~/.claude/sessions, ~/.claude/projects, ~/.codex). No API keys, no network requests, never modifies those files.\n- Install: download GoatBar-windows-x64.zip from https://github.com/Vantage-Connections-Org/goatbar/releases/latest , unzip, run GoatBar.exe (self-contained, no .NET install needed). Or build from source with install.ps1 (requires the .NET 8 SDK).\n- A macOS menu bar version is available as an early preview in the latest GitHub release (lightly tested). No Linux version.\n";
